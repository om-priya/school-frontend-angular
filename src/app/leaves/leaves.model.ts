export type LeaveData = {
  leave_date: string;
  leave_id: string;
  no_of_days: number;
  status: string;
  username: string;
};

export type CreateLeaveData = {
  leave_date: string;
  no_of_day: string;
};
